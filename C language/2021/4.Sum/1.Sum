/*
Program to print out the number of values entered and their sum
 
 written by Gioele Spanò -  ? ? 2021
 C18 tested with Codeblocks on Windows 10  64 bit
 */
 
#include <stdio.h>
int main()
{
	float num1, num2=0;
	int cont=0;
		do{
		printf("Enter a number: ");
		scanf_s("%f", &num1);
		num2 = num2 + num1;
		cont++;
		}while(num1>0);
            cont--;
			printf("The number of entered numbers is ", cont);
			printf("\nThe sum of the entered numbers is %.2f", num2);
	
	return 0;
}
